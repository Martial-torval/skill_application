{% extends "layout/default.html.twig" %}

{% block body %}

	<div class="profil">
		<h1>Mon profil</h1>

        <div>
		<h4>{{user.firstName}}</h4>
		<h4>{{user.lastName}}</h4>
		<h4>{{user.email}}</h4>
		<h4>{{user.phoneNumber}}</h4>
        <h4>{{user.password}}</h4>
		{# <h4>{{user.birthday}}</h4> #}
        </
	</div>
{% endblock %}
